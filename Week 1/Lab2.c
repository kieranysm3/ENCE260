#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    // there is no power function in c
    // the divide operator is like // in python if both are integers unless both are float then ok
    // or operator is || and and is &&
    // if not is  ! eg if (!(x + 1))
    //increment is ++
    // the meaning of x = y is different its an assignment expression that computes value of x after copying y
    //video2
    // if return value is 0 it is the same as False in python
int n = 6;
    if ((n > 5) - 1) {
        puts("Big");
    }
        else {
            puts("Small");
    }
}
// when printing unsinged integers, use %u instead of %d
